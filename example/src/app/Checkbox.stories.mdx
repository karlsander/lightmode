import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import { Checkbox } from "./Checkbox";
import { Theme } from "theme-ui-doc-blocks";
import { swiss, funk, base, github, future } from "@theme-ui/presets";
import ninersTheme from "../../../../staffbase-niners/src/gatsby-plugin-theme-ui/index.js";

<Meta title="Themes|All" />

# Theme

With `MDX` we can write about the theme right in the middle of our stories.

<Theme theme={base} />

<Theme theme={ninersTheme} />

<Theme theme={funk} />

<Theme theme={github} />
